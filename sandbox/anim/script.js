const uiSections = new Array(6);
let Explorer, Inspector, Workspace, Preview, Timeline;

function init() {
    uiSection.forEach(section => {
        section = document.createElement('div');
    });
}

window.onload = init;